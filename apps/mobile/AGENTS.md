# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Git Workflow

Always follow the git workflow defined in the root [AGENTS.md](../../AGENTS.md). Never push directly to `main`.
